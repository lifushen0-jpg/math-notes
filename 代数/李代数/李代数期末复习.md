# Lie Algebra 笔记整理：Ch1--Ch4
## 0. 参考书与主题
参考书：J. E. Humphreys, *Introduction to Lie Algebras and Representation Theory*.
主题：李代数的基本定义、线性李代数、经典李代数、导子、伴随表示、理想与商、可解与幂零、Engel 定理、Lie 定理、Jordan--Chevalley 分解与 Cartan 判别法。
## 1. 李代数的定义
设 $F$ 是域，$L$ 是 $F$ 上的线性空间，并有一个双线性运算 $$[\ ,\ ]:L\times L\to L,\qquad (x,y)\mapsto [x,y].$$ 若满足：
1. 双线性：$[\ ,\ ]$ is bilinear；
2. 交错性：$$[x,x]=0,\qquad \forall x\in L;$$
3. Jacobi 恒等式：$$[x,[y,z]]+[y,[z,x]]+[z,[x,y]]=0,\qquad \forall x,y,z\in L,$$
则称 $L$ 是 $F$ 上的李代数。
由 $[x,x]=0$ 可得 $$[x,y]=-[y,x].$$ 若 $\operatorname{char}F\neq 2$，则反交换性 $[x,y]=-[y,x]$ 与交错性 $[x,x]=0$ 等价。
## 2. 李代数同态、同构与子代数
设 $L_1,L_2$ 是李代数。若线性映射 $\varphi:L_1\to L_2$ 满足 $$\varphi([x,y])=[\varphi(x),\varphi(y)],\qquad \forall x,y\in L_1,$$ 则称 $\varphi$ 是李代数同态。
若 $\varphi$ 还是线性空间同构，则称 $\varphi$ 是李代数同构。
注意：全体李代数同态 $$\operatorname{Hom}_{Lie}(L_1,L_2)=\{\varphi:L_1\to L_2:\varphi\text{ 是李代数同态}\}$$ 通常不是线性空间，因为若 $\varphi$ 是同态，$k\varphi$ 一般不是同态：$$[k\varphi(x),k\varphi(y)]=k^2[\varphi(x),\varphi(y)]\neq k\varphi([x,y]).$$
若 $K\subseteq L$ 是线性子空间，且 $$[K,K]\subseteq K,$$ 则称 $K$ 是 $L$ 的李子代数。任取 $0\neq x\in L$，则 $Fx$ 是 $L$ 的一维子代数，因为 $[x,x]=0$。
## 3. 由结合代数构造李代数
设 $A$ 是一个 $F$-结合代数，定义 $$[x,y]=xy-yx,\qquad x,y\in A.$$ 则 $A$ 在该括号下成为李代数。
验证：
1. 双线性显然成立；
2. $$[x,x]=xx-xx=0;$$
3. Jacobi 恒等式由展开抵消得到：$$[x,[y,z]]+[y,[z,x]]+[z,[x,y]]=0.$$
特别地，若 $A=M_n(F)$，则 $$[M,N]=MN-NM$$ 使 $M_n(F)$ 成为李代数，记为 $$\mathfrak{gl}(n,F).$$ 更一般地，若 $V$ 是有限维 $F$-线性空间，则 $$\mathfrak{gl}(V)=\operatorname{End}(V)$$ 称为一般线性李代数。
## 4. 线性李代数
若 $L$ 是 $\mathfrak{gl}(V)$ 的李子代数，则称 $L$ 是线性李代数。
若 $\dim V=n$，选取 $V$ 的一组基后，有 $$\mathfrak{gl}(V)\cong\mathfrak{gl}(n,F).$$
设 $e_{ij}$ 是矩阵单位，即第 $(i,j)$ 位为 $1$，其余位置为 $0$。有 $$e_{ij}e_{kl}=\delta_{jk}e_{il},$$ 因此 $$[e_{ij},e_{kl}]=\delta_{jk}e_{il}-\delta_{li}e_{kj}.$$
该公式是后面计算经典李代数括号关系和根空间分解的基础。
## 5. 经典李代数
经典李代数主要分为四族：$A_\ell,B_\ell,C_\ell,D_\ell$。
### 5.1 $A_\ell$ 型：特殊线性李代数
令 $\dim V=\ell+1$，定义 $$\mathfrak{sl}(V)=\{x\in\mathfrak{gl}(V):\operatorname{Tr}x=0\}.$$ 在矩阵形式下，$$\mathfrak{sl}(\ell+1,F)=\{A\in\mathfrak{gl}(\ell+1,F):\operatorname{Tr}A=0\}.$$
因为 $\mathfrak{gl}(\ell+1,F)$ 的维数为 $(\ell+1)^2$，迹为零给出一个独立线性条件，所以 $$\dim\mathfrak{sl}(\ell+1,F)=(\ell+1)^2-1=\ell^2+2\ell.$$
一组常用基为 $$h_i=e_{ii}-e_{i+1,i+1},\qquad 1\leq i\leq \ell,$$ 以及所有非对角矩阵单位 $$e_{ij},\qquad i\neq j.$$
$A_\ell$ 的 Dynkin 图为 $$\circ-\circ-\cdots-\circ.$$
### 5.2 $C_\ell$ 型：辛李代数
令 $\dim V=2\ell$，取非退化反对称双线性型 $f$，其矩阵可取为 $$S=\begin{pmatrix}0&I_\ell\\-I_\ell&0\end{pmatrix}.$$ 定义辛李代数 $$\mathfrak{sp}(2\ell,F)=\{x\in\mathfrak{gl}(2\ell,F):f(xv,w)=-f(v,xw),\ \forall v,w\in V\}.$$ 矩阵形式为 $$\mathfrak{sp}(2\ell,F)=\{x\in\mathfrak{gl}(2\ell,F):Sx=-x^TS\}.$$
设 $$x=\begin{pmatrix}m&n\\p&q\end{pmatrix},$$ 则条件 $Sx=-x^TS$ 等价于 $$n^T=n,\qquad p^T=p,\qquad m^T=-q.$$ 因此 $$\dim\mathfrak{sp}(2\ell,F)=\ell^2+\frac{\ell(\ell+1)}2+\frac{\ell(\ell+1)}2=2\ell^2+\ell.$$
$C_\ell$ 的 Dynkin 图为 $$\circ\Leftarrow\circ-\circ-\cdots-\circ.$$
### 5.3 $B_\ell$ 型：奇维正交李代数
令 $\dim V=2\ell+1$，取非退化对称双线性型，其矩阵可取为 $$S=\begin{pmatrix}1&0&0\\0&0&I_\ell\\0&I_\ell&0\end{pmatrix}.$$ 定义 $$\mathfrak{o}(2\ell+1,F)=\{x\in\mathfrak{gl}(2\ell+1,F):Sx=-x^TS\}.$$ 它对应 $B_\ell$ 型，维数为 $$\dim\mathfrak{o}(2\ell+1,F)=2\ell^2+\ell.$$
$B_\ell$ 的 Dynkin 图为 $$\circ\Rightarrow\circ-\circ-\cdots-\circ.$$
### 5.4 $D_\ell$ 型：偶维正交李代数
令 $\dim V=2\ell$，取非退化对称双线性型，其矩阵可取为 $$S=\begin{pmatrix}0&I_\ell\\I_\ell&0\end{pmatrix}.$$ 定义 $$\mathfrak{o}(2\ell,F)=\{x\in\mathfrak{gl}(2\ell,F):Sx=-x^TS\}.$$ 它对应 $D_\ell$ 型，维数为 $$\dim\mathfrak{o}(2\ell,F)=2\ell^2-\ell.$$
$D_\ell$ 的 Dynkin 图末端分叉。
## 6. 三角矩阵李代数
记：
1. $\mathfrak{t}(n,F)$：全体上三角矩阵；
2. $\mathfrak{n}(n,F)$：全体严格上三角矩阵；
3. $\mathfrak{d}(n,F)$：全体对角矩阵。
维数为 $$\dim\mathfrak{t}(n,F)=\frac{n(n+1)}2,\qquad \dim\mathfrak{n}(n,F)=\frac{n(n-1)}2,\qquad \dim\mathfrak{d}(n,F)=n.$$
并且有线性空间直和 $$\mathfrak{t}(n,F)=\mathfrak{n}(n,F)\oplus\mathfrak{d}(n,F).$$
括号关系为 $$[\mathfrak{t}(n,F),\mathfrak{t}(n,F)]\subseteq\mathfrak{n}(n,F),\qquad [\mathfrak{d}(n,F),\mathfrak{n}(n,F)]\subseteq\mathfrak{n}(n,F).$$
事实上，在许多情形下有 $$[\mathfrak{t}(n,F),\mathfrak{t}(n,F)]=\mathfrak{n}(n,F).$$
## 7. 导子与导子李代数
设 $A$ 是一个 $F$-代数。线性映射 $\delta:A\to A$ 称为导子，如果 $$\delta(ab)=\delta(a)b+a\delta(b),\qquad \forall a,b\in A.$$
全体导子记为 $$\operatorname{Der}A=\{\delta\in\operatorname{Hom}_F(A,A):\delta\text{ 是导子}\}.$$
$\operatorname{Der}A$ 是 $\operatorname{Hom}_F(A,A)$ 的线性子空间。
若 $\delta_1,\delta_2\in\operatorname{Der}A$，定义 $$[\delta_1,\delta_2]=\delta_1\delta_2-\delta_2\delta_1,$$ 则 $[\delta_1,\delta_2]\in\operatorname{Der}A$，所以 $$\operatorname{Der}A$$ 是 $\mathfrak{gl}(A)$ 的李子代数。
## 8. 伴随表示与内导子
设 $L$ 是李代数，$x\in L$。定义 $$\operatorname{ad}x:L\to L,\qquad y\mapsto [x,y].$$
由 Jacobi 恒等式可得 $$\operatorname{ad}x([y,z])=[\operatorname{ad}x(y),z]+[y,\operatorname{ad}x(z)],$$ 因此 $$\operatorname{ad}x\in\operatorname{Der}L.$$
并且有 $$[\operatorname{ad}x,\operatorname{ad}y]=\operatorname{ad}[x,y].$$
所以映射 $$\operatorname{ad}:L\to\mathfrak{gl}(L),\qquad x\mapsto \operatorname{ad}x$$ 是李代数同态，称为伴随表示。
像空间 $$\operatorname{ad}L=\{\operatorname{ad}x:x\in L\}$$ 中的元素称为内导子；商空间意义下，$\operatorname{Der}L/\operatorname{ad}L$ 中的非零元素对应外导子。
注意：$$\operatorname{ad}x(x)=[x,x]=0,$$ 因此当 $x\neq 0$ 时，$\operatorname{ad}x$ 一般不可能是同构。
伴随表示的核为 $$\ker(\operatorname{ad})=Z(L).$$
若 $L$ 单且非交换，则 $Z(L)=0$，所以 $$\operatorname{ad}:L\to\mathfrak{gl}(L)$$ 是单射，因此单李代数可以看成线性李代数。
## 9. Ado 定理
Ado 定理：每个有限维李代数都同构于某个线性李代数。
也就是说，对任意有限维李代数 $L$，存在有限维线性空间 $V$ 与单射李代数同态 $$\rho:L\hookrightarrow\mathfrak{gl}(V).$$
该定理说明：有限维抽象李代数总可以通过矩阵表示来研究。
## 10. 阿贝尔李代数与结构常数
若 $$[x,y]=0,\qquad \forall x,y\in L,$$ 则称 $L$ 是阿贝尔李代数。
若 $\{x_1,\cdots,x_n\}$ 是 $L$ 的一组基，则可写 $$[x_i,x_j]=\sum_{k=1}^n a_{ij}^k x_k.$$ 系数 $a_{ij}^k$ 称为结构常数。
李代数公理转化为结构常数条件：
1. 交错性：$$a_{ii}^k=0;$$
2. 反交换性：$$a_{ij}^k+a_{ji}^k=0;$$
3. Jacobi 恒等式：$$\sum_{k=1}^n\left(a_{ij}^k a_{k\ell}^m+a_{j\ell}^k a_{ki}^m+a_{\ell i}^k a_{kj}^m\right)=0.$$
一维李代数必为阿贝尔李代数。
二维李代数的基本情形：若 $L$ 有基 $x,y$，且 $[x,y]=0$，则 $L$ 阿贝尔；若 $[x,y]\neq 0$，经过换基可以化为 $$[x,y]=x.$$
## 11. 理想
设 $L$ 是李代数，$I\subseteq L$ 是线性子空间。若 $$[x,y]\in I,\qquad \forall x\in L,\ y\in I,$$ 则称 $I$ 是 $L$ 的理想，记作 $$I\triangleleft L.$$
理想一定是子代数。
若 $\varphi:L\to L'$ 是李代数同态，则 $$\ker\varphi\triangleleft L.$$
证明：若 $y\in\ker\varphi$，$x\in L$，则 $$\varphi([x,y])=[\varphi(x),\varphi(y)]=[\varphi(x),0]=0,$$ 所以 $[x,y]\in\ker\varphi$。
## 12. 中心与导出代数
中心定义为 $$Z(L)=\{z\in L:[x,z]=0,\ \forall x\in L\}.$$ $Z(L)$ 是 $L$ 的理想。
有 $$L\text{ 阿贝尔}\Longleftrightarrow Z(L)=L.$$
导出代数定义为 $$[L,L]=\operatorname{span}\{[x,y]:x,y\in L\}.$$ $[L,L]$ 是 $L$ 的理想。
有 $$L\text{ 阿贝尔}\Longleftrightarrow [L,L]=0.$$
例子：$$[\mathfrak{gl}(n,F),\mathfrak{gl}(n,F)]=\mathfrak{sl}(n,F),$$ $$[\mathfrak{sl}(n,F),\mathfrak{sl}(n,F)]=\mathfrak{sl}(n,F).$$
## 13. 理想的基本性质与单李代数
若 $I,J$ 是 $L$ 的理想，则：
1. $$I+J\triangleleft L;$$
2. $$[I,J]\triangleleft L.$$
若 $L$ 的理想只有 $0$ 和 $L$，且 $L$ 非阿贝尔，即 $[L,L]\neq 0$，则称 $L$ 是单李代数。
若 $L$ 单，则 $$Z(L)=0,\qquad [L,L]=L.$$
例子：当 $\operatorname{char}F\neq 2$ 时，$$\mathfrak{sl}(2,F)$$ 是单李代数。
$\mathfrak{sl}(2,F)$ 的标准基为 $$x=\begin{pmatrix}0&1\\0&0\end{pmatrix},\qquad y=\begin{pmatrix}0&0\\1&0\end{pmatrix},\qquad h=\begin{pmatrix}1&0\\0&-1\end{pmatrix}.$$
括号关系为 $$[h,x]=2x,\qquad [h,y]=-2y,\qquad [x,y]=h.$$
## 14. 商李代数
若 $I\triangleleft L$，则商空间 $L/I$ 上定义括号 $$[\bar{x},\bar{y}]=\overline{[x,y]},\qquad \bar{x}=x+I,\ \bar{y}=y+I.$$ 该定义良好，因为若 $x'=x+a$，$y'=y+b$，其中 $a,b\in I$，则 $$[x',y']=[x+a,y+b]=[x,y]+[a,y]+[x,b]+[a,b],$$ 其中后三项均属于 $I$，所以 $$\overline{[x',y']}=\overline{[x,y]}.$$
于是 $L/I$ 是李代数，称为商李代数。
例子：$$\mathfrak{gl}(n,F)/\mathfrak{sl}(n,F)\cong F,$$ 因此该商李代数是阿贝尔的。
## 15. 正规化子与中心化子
设 $K$ 是 $L$ 的子代数。定义 $K$ 在 $L$ 中的正规化子为 $$N_L(K)=\{x\in L:[x,K]\subseteq K\}.$$ 则 $N_L(K)$ 是 $L$ 的子代数。
若 $$N_L(K)=K,$$ 则称 $K$ 是自正规化的。
设 $X\subseteq L$，定义 $X$ 在 $L$ 中的中心化子为 $$C_L(X)=\{x\in L:[x,X]=0\}.$$ $C_L(X)$ 是 $L$ 的子代数。
特别地，$$C_L(L)=Z(L).$$
## 16. 同构定理
设 $\varphi:L\to L'$ 是李代数同态。
第一同构定理：$$L/\ker\varphi\cong\operatorname{Im}\varphi.$$
更一般地，若 $I\triangleleft L$ 且 $I\subseteq\ker\varphi$，则存在唯一李代数同态 $$\psi:L/I\to L'$$ 使得下图交换：$$\varphi=\psi\circ\pi,$$ 其中 $\pi:L\to L/I$ 是自然投影。
第二同构定理：若 $I\subseteq J\triangleleft L$，则 $$J/I\triangleleft L/I,\qquad (L/I)/(J/I)\cong L/J.$$
第三同构定理：若 $I,J\triangleleft L$，则 $$I+J/I\cong J/(I\cap J).$$
原像定理：若 $\varphi:L\to L'$ 是李代数同态，$J\triangleleft L'$，则 $$\varphi^{-1}(J)\triangleleft L.$$
## 17. 李代数表示
设 $V$ 是线性空间。李代数 $L$ 的一个表示是李代数同态 $$\rho:L\to\mathfrak{gl}(V).$$
也就是说，$\rho$ 是线性的，并且满足 $$\rho([x,y])=[\rho(x),\rho(y)]=\rho(x)\rho(y)-\rho(y)\rho(x).$$
伴随表示是最重要的表示：$$\operatorname{ad}:L\to\mathfrak{gl}(L),\qquad x\mapsto\operatorname{ad}x.$$
其核为 $$\ker(\operatorname{ad})=Z(L).$$
若 $L$ 单，则 $Z(L)=0$，所以伴随表示是单射。
## 18. 自同构与指数映射
设 $L$ 是李代数。李代数自同构是双射李代数同态 $$\varphi:L\to L.$$
若 $L\subseteq\mathfrak{gl}(V)$，$g\in GL(V)$，且 $$gLg^{-1}\subseteq L,$$ 则 $$\varphi_g:L\to L,\qquad x\mapsto gxg^{-1}$$ 是 $L$ 的自同构。
若 $\delta\in\operatorname{Der}L$ 且 $\delta$ 幂零，则 $$\exp\delta=I+\delta+\frac{\delta^2}{2!}+\cdots$$ 是 $L$ 的自同构。
特别地，若 $x\in L$ 且 $\operatorname{ad}x$ 幂零，则 $$\exp(\operatorname{ad}x)$$ 是 $L$ 的自同构。
在线性李代数中有公式 $$\exp(\operatorname{ad}x)(y)=\exp(x)y\exp(-x).$$
若 $L=\mathfrak{sl}(2,F)$，取 $$x=\begin{pmatrix}0&1\\0&0\end{pmatrix},\qquad y=\begin{pmatrix}0&0\\1&0\end{pmatrix},\qquad h=\begin{pmatrix}1&0\\0&-1\end{pmatrix},$$ 则 $$\operatorname{ad}x(y)=[x,y]=h,\qquad \operatorname{ad}x(h)=[x,h]=-2x,\qquad \operatorname{ad}x(x)=0,$$ 因此 $\operatorname{ad}x$ 是幂零变换，$\exp(\operatorname{ad}x)$ 是 $\mathfrak{sl}(2,F)$ 的自同构。
## 19. 可解李代数
定义导出列：$$L^{(0)}=L,\qquad L^{(i+1)}=[L^{(i)},L^{(i)}].$$
若存在 $m$，使得 $$L^{(m)}=0,$$ 则称 $L$ 是可解李代数。
例子：若 $L$ 是阿贝尔李代数，则 $L^{(1)}=[L,L]=0$，所以 $L$ 可解。
若 $L=\mathfrak{t}(n,F)$，则 $$L^{(1)}=[\mathfrak{t},\mathfrak{t}]\subseteq\mathfrak{n}(n,F),$$ 且 $\mathfrak{n}(n,F)$ 经过有限次交换后为 $0$，所以 $\mathfrak{t}(n,F)$ 可解。
若 $L$ 单且非阿贝尔，则 $[L,L]=L$，从而 $$L^{(i)}=L,\qquad \forall i,$$ 因此 $L$ 不可解。
## 20. 可解根基与半单李代数
若 $I,J$ 是 $L$ 的可解理想，则 $I+J$ 仍是可解理想。
因此所有可解理想之和仍是可解理想，称为 $L$ 的可解根基，记作 $$\operatorname{Rad}L.$$
即 $$\operatorname{Rad}L=\text{$L$ 的最大可解理想}.$$
若 $$\operatorname{Rad}L=0,$$ 则称 $L$ 是半单李代数。
若 $L$ 是单李代数，则 $L$ 是半单李代数。
## 21. 幂零李代数
定义下中心列：$$L^0=L,\qquad L^i=[L,L^{i-1}],\quad i\geq 1.$$
若存在 $m$，使得 $$L^m=0,$$ 则称 $L$ 是幂零李代数。
幂零性比可解性更强：$$L\text{ 幂零}\Longrightarrow L\text{ 可解}.$$
例子：$\mathfrak{n}(n,F)$ 是幂零李代数，因为严格上三角矩阵不断取交换子后最终为零。
二维非阿贝尔李代数满足存在基 $x,y$ 使得 $$[x,y]=x.$$ 它是可解的，因为 $$L^{(1)}=Fx,\qquad L^{(2)}=[Fx,Fx]=0.$$ 但它不是幂零的，因为 $$[L,Fx]=Fx,$$ 下中心列不会变成 $0$。
## 22. Engel 定理
Engel 定理的线性形式：设 $L$ 是 $\mathfrak{gl}(V)$ 的李子代数，且 $L$ 中每个元素都是 $V$ 上的幂零线性变换，则存在 $0\neq v\in V$，使得 $$xv=0,\qquad \forall x\in L.$$
换言之，一族由李括号封闭的幂零线性变换有公共零向量。
Engel 定理的抽象形式：若有限维李代数 $L$ 满足对任意 $x\in L$，$\operatorname{ad}x$ 都是幂零线性变换，则 $L$ 是幂零李代数。
记号：若 $x\in L$，则 $$\operatorname{ad}x:L\to L,\qquad y\mapsto[x,y].$$
因此 Engel 定理给出判别：$$\forall x\in L,\ \operatorname{ad}x\text{ 幂零}\Longrightarrow L\text{ 幂零}.$$
## 23. Lie 定理
设 $F$ 是代数闭域，$\operatorname{char}F=0$。若 $L$ 是 $\mathfrak{gl}(V)$ 的可解李子代数，且 $0\neq V$ 是有限维线性空间，则存在 $0\neq v\in V$，使得 $$xv=\lambda(x)v,\qquad \forall x\in L,$$ 其中 $\lambda:L\to F$ 是线性函数。
等价地，$L$ 中所有元素可在某组基下同时表示为上三角矩阵。
推论：若 $L$ 可解，则 $[L,L]$ 在任意有限维表示中由幂零变换作用。特别地，若 $L\subseteq\mathfrak{gl}(V)$ 可解，则 $$[L,L]$$ 中每个元素都是幂零线性变换。
## 24. Jordan--Chevalley 分解
设 $V$ 是有限维线性空间，$x\in\mathfrak{gl}(V)$。在代数闭域上，$x$ 可唯一分解为 $$x=x_s+x_n,$$ 其中：
1. $x_s$ 是半单部分，即可对角化；
2. $x_n$ 是幂零部分；
3. $$x_sx_n=x_nx_s;$$
4. $x_s,x_n$ 都可表示为 $x$ 的多项式。
该分解称为 Jordan--Chevalley 分解。
若 $L\subseteq\mathfrak{gl}(V)$ 是半单线性李代数，则对任意 $x\in L$，其 Jordan--Chevalley 分解 $$x=x_s+x_n$$ 满足 $$x_s,x_n\in L.$$
这说明半单线性李代数对半单部分和幂零部分封闭。
## 25. 抽象 Jordan 分解
设 $L$ 是半单李代数。若 $x\in L$，则 $\operatorname{ad}x$ 有 Jordan--Chevalley 分解：$$(\operatorname{ad}x)=(\operatorname{ad}x)_s+(\operatorname{ad}x)_n.$$
在半单李代数中，可定义 $x$ 的抽象半单部分 $x_s$ 和抽象幂零部分 $x_n$，满足 $$x=x_s+x_n,\qquad \operatorname{ad}x_s=(\operatorname{ad}x)_s,\qquad \operatorname{ad}x_n=(\operatorname{ad}x)_n.$$
这将线性代数中的 Jordan 分解推广到抽象半单李代数。
## 26. Cartan 判别法与 Killing 型
设 $L$ 是有限维李代数。定义 Killing 型为 $$\kappa(x,y)=\operatorname{Tr}(\operatorname{ad}x\operatorname{ad}y),\qquad x,y\in L.$$
Killing 型是对称双线性型，并满足不变性：$$\kappa([x,y],z)=\kappa(x,[y,z]).$$
Cartan 可解性判别法：若 $$\kappa(x,y)=0,\qquad \forall x\in[L,L],\ y\in L,$$ 则 $L$ 可解。反过来，在特征 $0$ 条件下，若 $L$ 可解，也有上述迹条件。
Cartan 半单性判别法：$$L\text{ 半单}\Longleftrightarrow \kappa\text{ 非退化}.$$
即 $$L\text{ 半单}\Longleftrightarrow \operatorname{rad}\kappa=0,$$ 其中 $$\operatorname{rad}\kappa=\{x\in L:\kappa(x,y)=0,\ \forall y\in L\}.$$
## 27. 半单李代数的基本性质
若 $L$ 半单，则：
1. $$Z(L)=0;$$
2. $$[L,L]=L;$$
3. $L$ 没有非零阿贝尔理想；
4. $L$ 没有非零可解理想；
5. Killing 型非退化。
若 $I\triangleleft L$，定义正交补 $$I^\perp=\{x\in L:\kappa(x,I)=0\}.$$ 由 Killing 型不变性可知，$I^\perp$ 也是 $L$ 的理想。
半单李代数可分解为单理想的直和：$$L=L_1\oplus\cdots\oplus L_r,$$ 其中每个 $L_i$ 是单李代数，并且 $$[L_i,L_j]=0,\qquad i\neq j.$$
## 28. Cartan 判别法的常用线性版本
设 $L\subseteq\mathfrak{gl}(V)$ 是李子代数。若对任意 $x\in[L,L]$ 和 $y\in L$，都有 $$\operatorname{Tr}(xy)=0,$$ 则 $L$ 可解。
这是线性李代数版本的 Cartan 判别法。
在抽象李代数中，把 $\operatorname{Tr}(xy)$ 替换为 $$\operatorname{Tr}(\operatorname{ad}x\operatorname{ad}y)=\kappa(x,y).$$
## 29. 本份笔记中的核心结论清单
1. 李代数定义：双线性、交错性、Jacobi 恒等式。
2. 结合代数可通过 $$[x,y]=xy-yx$$ 构成李代数。
3. $$\mathfrak{gl}(V)=\operatorname{End}(V)$$ 是基本线性李代数。
4. 矩阵单位括号公式：$$[e_{ij},e_{kl}]=\delta_{jk}e_{il}-\delta_{li}e_{kj}.$$
5. 经典李代数四族：$$A_\ell=\mathfrak{sl}(\ell+1,F),\qquad B_\ell=\mathfrak{o}(2\ell+1,F),\qquad C_\ell=\mathfrak{sp}(2\ell,F),\qquad D_\ell=\mathfrak{o}(2\ell,F).$$
6. 导子构成李代数：$$\operatorname{Der}A\leq\mathfrak{gl}(A).$$
7. 伴随表示：$$\operatorname{ad}:L\to\mathfrak{gl}(L),\qquad x\mapsto\operatorname{ad}x.$$
8. $$\ker(\operatorname{ad})=Z(L).$$
9. Ado 定理：有限维李代数都可实现为线性李代数。
10. 理想、商李代数与同构定理构成基本结构工具。
11. 可解性由导出列刻画：$$L^{(0)}=L,\qquad L^{(i+1)}=[L^{(i)},L^{(i)}].$$
12. 幂零性由下中心列刻画：$$L^0=L,\qquad L^i=[L,L^{i-1}].$$
13. $$L\text{ 幂零}\Longrightarrow L\text{ 可解}.$$
14. Engel 定理：若所有 $\operatorname{ad}x$ 都幂零，则 $L$ 幂零。
15. Lie 定理：可解线性李代数可同时三角化。
16. Jordan--Chevalley 分解：$$x=x_s+x_n,\qquad [x_s,x_n]=0.$$
17. Killing 型：$$\kappa(x,y)=\operatorname{Tr}(\operatorname{ad}x\operatorname{ad}y).$$
18. Cartan 可解性判别法：$$L\text{ 可解}\Longleftrightarrow \kappa([L,L],L)=0.$$
19. Cartan 半单性判别法：$$L\text{ 半单}\Longleftrightarrow \kappa\text{ 非退化}.$$
20. 半单李代数可分解为单理想直和。
## 30. 学习主线总结
这份笔记的主线是：先从抽象定义出发建立李代数的语言，再通过 $\mathfrak{gl}(V)$ 和经典李代数理解具体模型；随后引入理想、商、同构定理和表示；再用可解、幂零、Engel 定理和 Lie 定理研究“接近上三角矩阵”的李代数；最后用 Jordan--Chevalley 分解、Killing 型和 Cartan 判别法进入半单李代数理论。