# 一. Jordan分解

## (1). 准素空间分解

设线性变换 $A$ 的极小多项式为 $m(\lambda)=\prod_{i=1}^kf_i(\lambda)^{e_i}$，特征多项式为 $f(\lambda)=\prod_{i=1}^kf_i(\lambda)^{f_i}$
$W_i=\ker f_i(A)$，那么：
- $W_i$ 是 $A$ 的不变子空间
- $V= \oplus_{i=1}^kW_i$
- $A_i=A|_{W_i}$ 的极小多项式为 $f_i(\lambda)^{e_i}$，特征多项式为 $f_i(\lambda)^{f_i}$
- 投影映射 $\epsilon_i:V\to W_i$ 是 $A$ 的多项式

那么，由线性变换与矩阵的同构性可得：
- 矩阵 $A$ 可相似于  $diag\{A_1,A_2,\cdots,A_k\}$，其中 $A_i$ 的极小多项式为 $f_i(\lambda)^{e_i}$，特征多项式为 $f_i(\lambda)^{f_i}$

## (2). 循环分解

我们先来给出循环子空间的定义与几个推论

### 循环子空间

**定义**
 设 $V$ 是域 $F$ 上 $n$ 维线性空间，$\mathscr{A}$ 是 $V$ 的线性变换. 固定 $\alpha \in V$, 记 $\alpha$ 生成的循环子空间为 $$W = F[\mathscr{A}]\alpha = F[\lambda]\alpha.$$
(1) 若 $\alpha, \mathscr{A}\alpha, \cdots, \mathscr{A}^{k-1}\alpha$ 线性无关，而 $\alpha, \mathscr{A}\alpha, \cdots, \mathscr{A}^k\alpha$ 线性相关，设为 $\mathscr{A}^k\alpha + c_{k-1}\mathscr{A}^{k-1}\alpha + \cdots + c_0\alpha = 0$, 则 $m_{\alpha}(\lambda) = \lambda^k + c_{k-1}\lambda^{k-1} + \cdots + c_0 \in F[\lambda]$ 是 $\alpha$ 的最小零化子.
(2) $W = F[\mathscr{A}]\alpha$ 的维数为 $k = \deg m_{\alpha}$, 且 $\alpha, \mathscr{A}\alpha, \cdots, \mathscr{A}^{k-1}\alpha$ 是 $W$ 的基.
(3) $\mathscr{A}_W = \mathscr{A}|_W$ 在上述基下的方阵表示为 $m_{\alpha}$ 的友阵$$A_W = \begin{bmatrix} 
0 & & & -c_0 \\ 
1 & \ddots & & \vdots \\ 
 & \ddots & 0 & -c_{k-2} \\ 
 & & 1 & -c_{k-1} 
\end{bmatrix}.$$
特别地，$\mathscr{A}_W$ 的最小多项式 $m_W$、特征多项式 $f_W$ 及 $\alpha$ 的最小零化子 $m_{\alpha}$ 三者相等，即$$m_W = f_W = m_{\alpha}.$$

### 循环分解定理

**循环分解 (cyclic decomposition)** 设 $V$ 是域 $F$ 上 $n$ 维线性空间，$\mathscr{A}$ 是 $V$ 的线性变换，则：

(1) 
$$V = F[\mathscr{A}]\alpha_1 \oplus \cdots \oplus F[\mathscr{A}]\alpha_r,$$
其中 $V_i = F[\mathscr{A}]\alpha_i$ 是 $\alpha_i \in V$ 生成的非 $0$ 循环子空间. 特别 $\mathscr{A}_i = \mathscr{A}|_{V_i}$ 的最小多项式 $m_i = m_i(\lambda)$、特征多项式 $f_i = f_i(\lambda)$ 及 $\alpha_i$ 的最小零化子 $m_{\alpha_i} = m_{\alpha_i}(\lambda)$ 三者相等，即 $m_i = f_i = m_{\alpha_i} \ (1 \leqslant i \leqslant r)$;

(2) $m_i \mid m_{i-1} \ (i = 2,3, \cdots, r)$, 且 $m_1, \cdots, m_r$ 由 $\mathscr{A}$ 唯一决定（称为 $\mathscr{A}$ 的**不变因子 (invariant factors)**）. $\mathscr{A}$ 的最小多项式为 $m(\lambda) = m_1$，特征多项式为
$$f(\lambda) = m_1 \cdots m_r.$$
**方阵的有理标准形（rational form），循环标准形**
对域 $F$ 上任一方阵 $A$, 存在着 $F$ 上可逆方阵 $P$ 使得 $$B = P^{-1}AP = \text{diag}(\text{C}(m_1), \cdots, \text{C}(m_r)),$$ 其中 $\text{C}(m_i)$ 是 $m_i \in F[\lambda]$ 的友阵，且 $m_i \mid m_{i-1} \ (i = 2, \cdots, r)$. $m_1, \cdots, m_r$ 由 $A$ 唯一决定（称为 $A$ 的不变因子）. 特别地，$A$ 的最小多项式为 $m_1$, 特征多项式为 $f(\lambda) = m_1 \cdots m_r$（方阵 $B$ 称为 $A$ 的**有理标准形**）.


### Jordan 分解

我们开考虑 $\mathbb{C}$ 上的 Jordan 分解
我们先进行准素分解，得到的 $A_i$ 只有一个特征值 $\lambda_i$
然后进行循环分解，设 $A_i = P \text{diag}\{A_{i1},\cdots,A_{is}\}P^{-1}$，其中，$A_{is}$ 与 $A_i$ 有相同的极小多项式，所有 $A_{ij}$ 的特征多项式的乘积为 $A_i$ 的特征多项式
由于所有的 $A_{ij}$ 都只有特征值 $\lambda_i$，那么 $A_{ij}-\lambda_{i} I_{ij}$ 的特征多项式与极小多项式都为 $\lambda^{k_{ij}}$，其相似于友阵
$$\begin{bmatrix} 0 & & & 0 \\ 1 & \ddots & & \vdots \\ & \ddots & 0 &0 \\ & & 1 & 0 \end{bmatrix}$$
那么，$A_{ij}$ 相似于
$$\begin{bmatrix} 
\lambda_i  & & &  \\ 
1 & \ddots & &  \\ 
 & \ddots & \lambda_i  &  \\ 
 & & 1 & \lambda_i 
\end{bmatrix}.$$

上述矩阵称为 Jordan 块 $J_{ij}$，所有 Jordan 块组成的分块对角阵与原矩阵相似，称为矩阵的 Jordan 标准型

现在看 Jordan 标准形的几何意义. 设 $V$ 是域 $F$ 上 $n$ 维线性空间，$\mathscr{A}$ 是 $V$ 的线性变换，设 $\mathscr{A}$ 在 $F$ 中有 $n$ 个特征根（重根计入）（特别当 $F = \mathbb{C}$ 为复数域时总是这样）. 设 $\mathscr{A}$ 在基 $\varepsilon_1, \cdots, \varepsilon_n$ 下的方阵表示为 $A$，于是 $\mathscr{A}$ 和 $A$ 的特征多项式为 $f(\lambda) = (\lambda - \lambda_1)^{d_1} \cdots (\lambda - \lambda_s)^{d_s}$. 将 $V$ 作准素分解：$V = W_1 \oplus \cdots \oplus W_s$, 再将 $W_i$ 作循环分解，则
$$V = \bigoplus_{i=1}^s W_i = \bigoplus_{i,j} W_{ij},$$
$\mathscr{A}$ 在 $W_i$ 和 $W_{ij}$ 上限制为 $\mathscr{A}_i$ 和 $\mathscr{A}_{ij}$, 特征多项式为 $(\lambda - \lambda_i)^{d_i}$ 和 $(\lambda - \lambda_i)^{k_{ij}}$. 任取一个 $W_{ij}$, 记 $W_{ij} = W, k_{ij} = k$. 因 $W$ 是 $\mathscr{A}$ 的循环子空间，故 $W = F[\lambda]\alpha$, $\alpha$ 的最小零化子为 $(\lambda - \lambda_i)^k$. 记 $\alpha = \alpha_1$ 及
$$(\lambda - \lambda_i)\alpha = \alpha_2, \quad (\lambda - \lambda_i)^2\alpha = \alpha_3, \quad \cdots, \quad (\lambda - \lambda_i)^{k-1}\alpha = \alpha_k, \quad (\lambda - \lambda_i)^k\alpha = 0,$$
即
$$(\lambda - \lambda_i)\alpha_1 = \alpha_2, \quad (\lambda - \lambda_i)\alpha_2 = \alpha_3, \quad \cdots, \quad (\lambda - \lambda_i)\alpha_{k-1} = \alpha_k, \quad (\lambda - \lambda_i)\alpha_k = 0.$$
亦即
$$\mathscr{A}\alpha_1 = \lambda_i\alpha_1 + \alpha_2, \quad \mathscr{A}\alpha_2 = \lambda_i\alpha_2 + \alpha_3, \quad \cdots, \quad \mathscr{A}\alpha_{k-1} = \lambda_i\alpha_{k-1} + \alpha_k, \quad \mathscr{A}\alpha_k = \lambda_i\alpha_k. \qquad (*)$$

满足 $(*)$ 式的向量 $\alpha_1, \alpha_2, \cdots, \alpha_k$ 称为一个“Jordan 链”. 这样的 Jordan 链构成 $W$ 的基. 在此基下 $\mathscr{A}_{ij}$ 的方阵表示为 Jordan 块 $J_k(\lambda_i)$. 当然这也可由定理 7.10 中方阵 $A$ 的 Jordan 标准形看出. 以 $W_{ij} = W_{11}$ 为例，由 $P^{-1}AP = J = \text{diag}(J_1, \cdots, J_t)$, 知 $J_1$ 的各列为 $(\lambda_1, 1, 0, \cdots, 0)^{\mathrm{T}}, \cdots, (0, \cdots, \lambda_1)^{\mathrm{T}}$. 这应是新基 $\alpha_1, \cdots, \alpha_k$ 在 $\mathscr{A}$ 作用下象的（新）坐标列，故知 $\mathscr{A}\alpha_1 = \lambda_1\alpha_1 + \alpha_2, \cdots, \mathscr{A}\alpha_k = \lambda_1\alpha_k$. 

## Jordan块与极小多项式

首先，我们先来研究特征值 $\lambda$ 对应的 Jordan 块与其几何重数和代数重数的关系

### 与代数重数

我们知道，特征值的代数重数是其作为特征多项式的根的重数
由于相似矩阵有相同的特征多项式，我们不难发现，几何重数应该是该特征值对应的所有Jordan块的阶数之和

举例，如果一个矩阵有以下Jordan块：
$\begin{pmatrix}\lambda &1 & & \\ & \lambda &1 & \\& & \lambda &1 \\& & & \lambda\end{pmatrix}$，$\begin{pmatrix}\lambda &1 \\ & \lambda\end{pmatrix}$，$(\lambda)$
那么，矩阵的特征值 $\lambda$ 的几何重数为7

### 与几何重数

特征值 $\lambda$ 的几何重数是方程 $(\lambda I - A)x=0$ 的解空间的维数
等价于将 $A$ 化为Jordan块后的方程的解空间的维数 $(\lambda I - J)x=0$
我们将 $J$ 限制到特征值 $\lambda$ 对应的 Jordan块上：$(\lambda I-J_k(\lambda))x=0$

对于其中一个 k 阶的Jordan块，其秩为 k-1，解空间的维数为 1，那么一个 Jordan块就只对应一维的解空间
故而 $\lambda$ 的几何重数为 Jordan块的个数

在上面的例子中，$\lambda$ 对应三个 Jordan 块，$\lambda$ 的几何重数为3

### 与极小多项式

我们知道，$m(A)=0$ $\iff$ $m(J)=0$
那么将每个 $J_k(\lambda_i)$ 带入，那么应该也得到 $m(J_k(\lambda_i))=0$

但是，根据 Jordan块的性质，我们要求 $m(\lambda)$ 的项 $(\lambda-\lambda_i)$ 的次数要不小于特征值 $\lambda_i$ 对应的 Jordan块中阶数的最大者
也就是说，特征值 $\lambda$ 对应的 Jordan块的最大阶数等于其作为极小多项式的根的重数

# 矩阵交换性
## 1. 交换性诱导同时对角化
## 2. 特征向量空间互为不变子空间
**证明**：
设 $V_0$ 是 $\phi$ 的特征向量空间，记 $v\in V_0:\phi v = \lambda v$
$(\psi \phi) v =\psi(\lambda v) = \lambda (\psi v)$
$(\psi \phi) v=\phi(\psi v)= \lambda(\psi v)$
所以 $\psi \in V_0$

## 3. 有相同特征向量

