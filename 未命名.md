## 循环分解定理
设 $V$ 是域 $F$ 上的有限维线性空间，$T\in\operatorname{End}(V)$。把 $V$ 看成 $F[x]$-模，其中多项式 $f(x)$ 对向量 $v$ 的作用定义为 $$f(x)\cdot v=f(T)v.$$ 对任意 $0\ne\alpha\in V$，称 $$Z(\alpha;T)=F[x]\alpha=\operatorname{span}\{\alpha,T\alpha,T^2\alpha,\cdots\}$$ 为由 $\alpha$ 生成的 $T$-循环子空间。
### 1. 循环子空间与零化多项式
若存在最小次数首一多项式 $$p_\alpha(x)=x^r+a_{r-1}x^{r-1}+\cdots+a_0$$ 使得 $$p_\alpha(T)\alpha=0,$$ 则称 $p_\alpha(x)$ 为 $\alpha$ 关于 $T$ 的零化多项式。此时 $$\alpha,T\alpha,\cdots,T^{r-1}\alpha$$ 线性无关，并且 $$Z(\alpha;T)=\operatorname{span}\{\alpha,T\alpha,\cdots,T^{r-1}\alpha\}.$$ 因此 $$\dim Z(\alpha;T)=\deg p_\alpha(x).$$ 在这组基下，$T|_{Z(\alpha;T)}$ 的矩阵是多项式 $p_\alpha(x)$ 的友矩阵。
### 2. 循环分解定理的表述
设 $V$ 是有限维 $F[x]$-模，也就是 $V$ 上给定线性变换 $T$。则存在向量 $\alpha_1,\alpha_2,\cdots,\alpha_s\in V$，使得 $$V=Z(\alpha_1;T)\oplus Z(\alpha_2;T)\oplus\cdots\oplus Z(\alpha_s;T).$$ 并且若 $p_i(x)$ 是 $\alpha_i$ 的零化多项式，则可以要求 $$p_1(x)\mid p_2(x)\mid\cdots\mid p_s(x).$$ 这些多项式 $p_1(x),\cdots,p_s(x)$ 称为 $T$ 的不变因子。这个分解称为 $V$ 关于 $T$ 的循环分解。
### 3. 定理的矩阵形式
若 $$V=Z(\alpha_1;T)\oplus\cdots\oplus Z(\alpha_s;T),$$ 且 $$p_i(x)=x^{r_i}+a_{i,r_i-1}x^{r_i-1}+\cdots+a_{i,0},$$ 则在基 $$\alpha_1,T\alpha_1,\cdots,T^{r_1-1}\alpha_1;\ \cdots;\ \alpha_s,T\alpha_s,\cdots,T^{r_s-1}\alpha_s$$ 下，$T$ 的矩阵为分块对角矩阵 $$[T]=C(p_1)\oplus C(p_2)\oplus\cdots\oplus C(p_s),$$ 其中 $C(p_i)$ 是 $p_i(x)$ 的友矩阵。若采用基 $$\alpha_i,T\alpha_i,\cdots,T^{r_i-1}\alpha_i,$$ 则友矩阵可以写为 $$C(p_i)=\begin{pmatrix}0&0&\cdots&0&-a_{i,0}\\1&0&\cdots&0&-a_{i,1}\\0&1&\cdots&0&-a_{i,2}\\\vdots&\vdots&\ddots&\vdots&\vdots\\0&0&\cdots&1&-a_{i,r_i-1}\end{pmatrix}.$$
### 4. 与有理标准型的关系
循环分解定理说明：任意线性变换 $T$ 都可以分解为若干循环子空间上的直和。在每个循环子空间上，$T$ 的矩阵是一个友矩阵。因此循环分解定理直接给出有理标准型：$$[T]\sim C(p_1)\oplus C(p_2)\oplus\cdots\oplus C(p_s),$$ 其中 $$p_1(x)\mid p_2(x)\mid\cdots\mid p_s(x).$$ 这个分块对角矩阵就是 $T$ 的有理标准型。
### 5. 极小多项式和特征多项式
在循环分解 $$V=Z(\alpha_1;T)\oplus\cdots\oplus Z(\alpha_s;T)$$ 中，如果不变因子满足 $$p_1(x)\mid p_2(x)\mid\cdots\mid p_s(x),$$ 那么 $T$ 的极小多项式为最后一个不变因子：$$m_T(x)=p_s(x).$$ $T$ 的特征多项式为所有不变因子的乘积：$$\chi_T(x)=p_1(x)p_2(x)\cdots p_s(x).$$
### 6. 与准素分解的关系
准素分解是先按极小多项式的不同不可约因子分解空间。若 $$m_T(x)=q_1(x)^{e_1}\cdots q_t(x)^{e_t},$$ 则 $$V=\ker q_1(T)^{e_1}\oplus\cdots\oplus\ker q_t(T)^{e_t}.$$ 循环分解则是在每个准素分量内部继续分解成循环子空间。也就是说，准素分解是“按不可约因子分块”，循环分解是“把每一块进一步分解成由一个向量生成的循环块”。
### 7. 复数域上的理解
若 $F=\mathbb C$，则不可约多项式都是一次多项式。此时循环分解与 Jordan 标准型密切相关。一个 Jordan 块本质上就是一个循环子空间。若 $$J_r(\lambda)=\begin{pmatrix}\lambda&1&0&\cdots&0\\0&\lambda&1&\cdots&0\\\vdots&\vdots&\ddots&\ddots&\vdots\\0&0&\cdots&\lambda&1\\0&0&\cdots&0&\lambda\end{pmatrix},$$ 则它对应的循环子空间的零化多项式为 $$(x-\lambda)^r.$$ 因此在复数域上，循环分解可以看作 Jordan 分解的抽象形式。
### 8. 简单例子
若 $T$ 的有理标准型为 $$[T]\sim C(p_1)\oplus C(p_2),$$ 其中 $$p_1(x)=x-1,\qquad p_2(x)=(x-1)^2(x+2),$$ 且 $$p_1(x)\mid p_2(x),$$ 则存在 $\alpha_1,\alpha_2\in V$，使得 $$V=Z(\alpha_1;T)\oplus Z(\alpha_2;T),$$ 并且 $$p_{\alpha_1}(x)=x-1,\qquad p_{\alpha_2}(x)=(x-1)^2(x+2).$$ 此时 $$m_T(x)=p_2(x)=(x-1)^2(x+2),$$ $$\chi_T(x)=p_1(x)p_2(x)=(x-1)^3(x+2).$$
### 9. 总结
循环分解定理的核心结论是：有限维线性空间 $V$ 可以分解为若干个 $T$-循环子空间的直和，即 $$\boxed{V=Z(\alpha_1;T)\oplus\cdots\oplus Z(\alpha_s;T).}$$ 每个循环子空间由一个向量生成：$$Z(\alpha_i;T)=\operatorname{span}\{\alpha_i,T\alpha_i,\cdots,T^{r_i-1}\alpha_i\}.$$ 在相应基下，$T$ 的矩阵是若干友矩阵的直和：$$\boxed{[T]\sim C(p_1)\oplus\cdots\oplus C(p_s).}$$ 若进一步要求 $$p_1(x)\mid p_2(x)\mid\cdots\mid p_s(x),$$ 则得到有理标准型，其中 $$\boxed{m_T(x)=p_s(x),\qquad \chi_T(x)=p_1(x)\cdots p_s(x).}$$