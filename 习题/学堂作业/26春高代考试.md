# 第一次期中考试

## 1.（25分）

设 $A\in M_n(\mathbb C)$，$\lambda I-A$ 为特征矩阵。设 $D_1(\lambda),\dots,D_n(\lambda)$ 为行列式因子，$d_1(\lambda),\dots,d_n(\lambda)$ 为不变因子，其中

$$
d_i(\lambda)=\frac{D_i(\lambda)}{D_{i-1}(\lambda)}.
$$

证明：

1. $c(A)=D_n(\lambda)$；
2. $m(A)=d_n(\lambda)$；
3. $m(A)\mid c(A)\mid m(A)^n$；
4. 下列命题等价：
   - $m(A)=c(A)$；
   - 每个特征值的几何重数为 $1$；
   - 每个特征值对应恰好一个 Jordan 块；
   - $A$ 的不变子空间个数有限；
   - $A$ 相似于一个 Frobenius 块（友矩阵）；
5. 特征值 $\lambda$ 对应初等因子的最高次数 = 最大 Jordan 块阶数 = $(x-\lambda)$ 在极小多项式中的次数；
6. 特征值 $\lambda$ 对应初等因子的次数和 = Jordan 块阶数和 = $(x-\lambda)$ 在特征多项式中的次数。

---

## 2.（15分）

### (a) Bessel 不等式

设 $e_1,\dots,e_m$ 为 $V$ 中两两正交且模长为 $1$ 的向量，证明

$$
\sum_{i=1}^{m}|\langle v,e_i\rangle|^2\le \|v\|^2,\qquad \forall v\in V.
$$

### (b)

设 $e_1,\dots,e_n$ 为 $V$ 的一组标准正交基，证明

$$
\langle u,v\rangle
=
\sum_{i=1}^{n}
\langle u,e_i\rangle\overline{\langle v,e_i\rangle},
\qquad \forall u,v\in V.
$$

### (c) 离散傅里叶变换

设

$$
z=(z_0,z_1,\dots,z_{n-1})\in\mathbb C^n,
$$

定义

$$
\omega_j(z)
=
\frac1{\sqrt n}
\sum_{m=0}^{n-1}
z_m e^{-2\pi ijm/n},
\qquad j=0,\dots,n-1.
$$

离散傅里叶变换定义为

$$
Fz=(\omega_0(z),\dots,\omega_{n-1}(z)).
$$

证明：

1. $F$ 是 $\mathbb C^n$ 上的酉算子；
2. 若 $z_n=z_0$，则

   $$
   F^{-1}(z_0,z_1,\dots,z_{n-1})
   =
   F(z_n,z_{n-1},\dots,z_1);
   $$

3. $F^4=I$。

---

## 3.（10分）

设 $F$ 为 $M_n(\mathbb C)$ 的一个子空间，满足
$$
AB-BA\in F,\qquad \forall A,B\in F.
$$
若 $F$ 由幂零矩阵生成（即 $F$ 中每个矩阵均为幂零矩阵），证明存在可逆矩阵 $P$，使得对任意 $A\in F$，
$$
P^{-1}AP
$$
均为严格上三角矩阵。

---
## 4.（6分）
设
$$
A,B\in M_n(\mathbb R).
$$
若 $A,B$ 在 $\mathbb C$ 上相似，证明 $A,B$ 在 $\mathbb R$ 上也相似。

---

## 5.（7分）
设 $A,B$ 为 $n$ 阶复矩阵，满足
$$
I_n-A^*A>0,\qquad I_n-B^*B>0.
$$
证明
$$
\left|\det(I_n-A^*B)\right|^2
\ge
\det(I_n-A^*A)\det(I_n-B^*B).
$$
---

## 6.（7分）
设
$$
A,B\in\mathbb Q^{n\times n}
$$
满足
$$
A^2-2AB-B^2=0.
$$
证明
$$
AB-BA
$$
不可逆。

---

## 7.（7分）
设 $A$ 为 $n$ 阶实对称正定矩阵，$Q$ 为 $n$ 阶正交矩阵。证明
$$
\operatorname{tr}(AQ)\le \operatorname{tr}(A),
$$
且等号成立当且仅当
$$
Q=I_n.
$$
---

## 8.（10分）
设
$$
M=
\begin{pmatrix}
B&C\\
C'&D
\end{pmatrix}
>0
$$
为 $(r+s)$ 阶正定矩阵，其最大特征值为 $\lambda_0$；$B,D$ 的最大特征值分别为 $\lambda_1,\lambda_2$。
证明
$$
\lambda_0<\lambda_1+\lambda_2.
$$
---
## 9.（10分）
设 $A$ 为 $n$ 阶实对称矩阵，$0\neq w\in\mathbb R^n$，且对任意非零向量 $y$，
$$
y^Tw=0
\Longrightarrow
y^TAy>0.
$$
证明矩阵
$$
\begin{pmatrix}
A&w\\
w^T&0
\end{pmatrix}
$$
恰有一个负特征值。

---

## 10.（6分）
设 $A=(a_{ij})$ 为 $n$ 阶实矩阵，满足
$$
a_{ii}=0,\qquad i=1,\dots,n,
$$
以及
$$
a_{ij}+a_{ji}=1,
\qquad
1\le i<j\le n.
$$
证明

$$
\operatorname{rank}(A)\ge n-1.
$$

---

## 11.（7分）
设 $A$ 为秩 $r$ 的 $n$ 阶实对称矩阵，其正惯性指数与负惯性指数分别为 $p,q$。令
$$
V=\{x\in\mathbb R^n:x^TAx=0\}.
$$
证明：包含于 $V$ 中的线性子空间的最大维数为
$$
\min(p,q)+n-r.
$$
---

## 12.（10分）
设 $V=M_n(\mathbb R)$，$\sigma:V\to V$ 为非零线性变换。若对任意 $A,B\in V$ 都有
$$
\sigma(AB)=\sigma(A)\sigma(B),
$$
或
$$
\sigma(AB)=\sigma(B)\sigma(A),
$$
证明存在可逆矩阵 $C$，使得以下两种情况之一成立：
1. 内自同构形式
   $$
   \sigma(A)=C^{-1}AC,
   \qquad \forall A\in V;
   $$
2. 转置型自同构形式
   $$
   \sigma(A)=C^{-1}A^TC,
   \qquad \forall A\in V.
   $$

# 解答与总结

## 1.

### 定义回顾
#### 行列式因子组与不变因子组

设 $A\in M_n(\mathbb F)$，考虑特征矩阵 $$\lambda I-A.$$
#### 行列式因子组（Determinantal Divisors）
对 $k=1,2,\dots,n$，记 $D_k(\lambda)$ 为 $\lambda I-A$ 的所有 $k$ 阶子式的最大公因式（首一化），并规定
$$D_0(\lambda)=1.$$
则
$$D_0(\lambda)\mid D_1(\lambda)\mid D_2(\lambda)\mid\cdots\mid D_n(\lambda).$$
称
$$D_1(\lambda),D_2(\lambda),\dots,D_n(\lambda)$$
为矩阵 $\lambda I-A$ 的**行列式因子组**。
特别地，
$$D_n(\lambda)=\det(\lambda I-A).$$
---

#### 不变因子组（Invariant Factors）

由行列式因子的整除链可定义

$$d_i(\lambda)=\frac{D_i(\lambda)}{D_{i-1}(\lambda)},\qquad i=1,\dots,n.$$
于是
$$D_k(\lambda)=d_1(\lambda)d_2(\lambda)\cdots d_k(\lambda).$$
并满足
$$d_1(\lambda)\mid d_2(\lambda)\mid\cdots\mid d_n(\lambda).$$
称
$$d_1(\lambda),d_2(\lambda),\dots,d_n(\lambda)$$
为矩阵 $\lambda I-A$ 的**不变因子组**。
若某些 $d_i(\lambda)=1$，通常将其略去。

---

#### 与有理标准形的关系

设非平凡不变因子为
$$d_{i_1}(\lambda),\dots,d_{i_r}(\lambda).$$
则
$$A\sim\operatorname{diag}\bigl(C(d_{i_1}),\dots,C(d_{i_r})\bigr),$$
其中 $C(f)$ 为多项式 $f$ 的友矩阵（Frobenius块）。

因此：

- 最大不变因子决定极小多项式；
- 全体不变因子的乘积决定特征多项式。

---

### 题目证明

设
$$D_1(\lambda),\dots,D_n(\lambda)$$
为行列式因子，
$$d_i(\lambda)=\frac{D_i(\lambda)}{D_{i-1}(\lambda)}$$
为不变因子。

---

#### 1. 证明 $c(A)=D_n(\lambda)$
由定义，
$$D_n(\lambda)$$
就是特征矩阵 $\lambda I-A$ 的唯一 $n$ 阶子式，即
$$D_n(\lambda)=\det(\lambda I-A).$$
而特征多项式定义为
$$c(A)=\det(\lambda I-A).$$
故
$$c(A)=D_n(\lambda).$$
---

#### 2. 证明 $m(A)=d_n(\lambda)$

根据有理标准形理论，
$$A\sim\operatorname{diag}(C(d_1),\dots,C(d_n)).$$
矩阵的极小多项式等于各块极小多项式的最小公倍式。

而由
$$d_1\mid d_2\mid\cdots\mid d_n,$$
有
$$\operatorname{lcm}(d_1,\dots,d_n)=d_n.$$
故
$$m(A)=d_n(\lambda).$$
---

#### 3. 证明
$$m(A)\mid c(A)\mid m(A)^n.$$
由(1)(2)
$$c(A)=D_n=d_1d_2\cdots d_n,$$
$$m(A)=d_n.$$
又因为
$$d_i\mid d_n\qquad(i=1,\dots,n),$$
故
$$c(A)=d_1d_2\cdots d_n\mid d_n^n=m(A)^n.$$
另一方面
$$m(A)=d_n\mid d_1d_2\cdots d_n=c(A).$$
于是
$$m(A)\mid c(A)\mid m(A)^n.$$

---

#### 4. 证明下列命题等价

**(i) $m(A)=c(A)$
(ii) 每个特征值的几何重数均为 $1$
(iii) 每个特征值恰对应一个 Jordan 块
(iv) $A$ 的不变子空间个数有限
(v) $A$ 相似于一个 Frobenius 块**


##### (i)$\Rightarrow$(v)
由
$$c(A)=d_1d_2\cdots d_n,$$
$$m(A)=d_n.$$
若
$$m(A)=c(A),$$
则
$$d_1=\cdots=d_{n-1}=1.$$
因此只有一个非平凡不变因子，
故有理标准形只有一个友矩阵块，
即
$$A\sim C(m(A)).$$
故 (v) 成立。

##### (v)$\Rightarrow$(i)
若
$$A\sim C(f),$$
则唯一非平凡不变因子就是 $f$，
故
$$m(A)=f=c(A).$$
##### (v)$\Leftrightarrow$(iii)
一个 Frobenius 块对应唯一的不变因子。
把不变因子进一步分解为初等因子后，
对于每个特征值只能出现一条 Jordan 链，
即恰有一个 Jordan 块。
反之若每个特征值只有一个 Jordan 块，
则对应每个特征值只有一个初等因子，
从而只有一个不变因子，
故矩阵相似于单个 Frobenius 块。

##### (iii)$\Leftrightarrow$(ii)

Jordan 标准形中，
某特征值对应 Jordan 块个数= 该特征值的几何重数。
因此
- 几何重数为 $1$
- Jordan 块个数为 $1$
二者等价。

---
##### (ii)$\Leftrightarrow$(iv)
设 Jordan 形中某特征值对应两个及以上 Jordan 块。
则可以任取这些块生成的部分直和，
得到无穷多个不变子空间。
故不变子空间个数无限。
反之，
若每个特征值只有一个 Jordan 块，
则不变子空间只能由 Jordan 链前缀产生，
其个数有限。
故
$$(ii)\Leftrightarrow(iv).$$
---
综上
$$(i)\Leftrightarrow(ii)\Leftrightarrow(iii)\Leftrightarrow(iv)\Leftrightarrow(v).$$
---
#### 5. 证明

特征值 $\lambda$ 对应初等因子的最高次数 $=$ 最大 Jordan 块阶数 $=$ $(x-\lambda)$ 在极小多项式中的次数
设
$$m_A(x)=\prod_{\lambda}(x-\lambda)^{s_\lambda}.$$
Jordan 标准形中，
对于特征值 $\lambda$，
设最大 Jordan 块阶数为 $r$。
则
$$(J_r(\lambda)-\lambda I)^r=0,$$
而
$$(J_r(\lambda)-\lambda I)^{r-1}\neq0.$$
故消去该块所需的最小指数为 $r$。
因此
$$s_\lambda=r.$$
另一方面，
初等因子恰为
$$(x-\lambda)^{r_1},\dots,(x-\lambda)^{r_t},$$
其最高次数

$$\max r_i=r.$$

故三者相等。

---

#### 6. 证明

特征值 $\lambda$ 对应初等因子的次数和 $=$ Jordan 块阶数和 $=$ $(x-\lambda)$ 在特征多项式中的次数
设对应 Jordan 块阶数为
$$r_1,\dots,r_t.$$
则对应初等因子为
$$(x-\lambda)^{r_1},\dots,(x-\lambda)^{r_t}.$$
故初等因子次数和为
$$r_1+\cdots+r_t.$$
而特征多项式中
$$(x-\lambda)$$

的指数恰等于该特征值的代数重数，
即所有 Jordan 块阶数之和：
$$a_\lambda=r_1+\cdots+r_t.$$
因此
$$\text{初等因子次数和}
=\text{Jordan块阶数和}
=\text{代数重数}.$$
即
$$\text{初等因子次数和}
=(x-\lambda)\text{ 在 }c(A)\text{ 中的次数}.$$
